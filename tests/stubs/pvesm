#!/usr/bin/env bash
# Test stub for pvesm. `pvesm path <volid>` echoes $PVESM_PATH when set;
# everything else fails, exactly as on a CI runner without Proxmox.
set -euo pipefail
if [[ "${1:-}" == "path" && -n "${PVESM_PATH:-}" ]]; then
    echo "$PVESM_PATH"
    exit 0
fi
exit 1
